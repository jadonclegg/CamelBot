// Game module built for CamelBot
// Authored by jkcoxson
// Long live camels

const { EventEmitter } = require('events');

module.exports = class game extends EventEmitter {
    constructor(client) {
        super();

    }
}